<!-- <p id="notice"><%= notice %></p> -->

<br><br>
<div id="publication-show" class="center-eighty">

	<p>
	  <strong>Title:</strong>
	  <%= @publication.title %>
	</p>

	<p>
	  <strong>Link:</strong>
	  <%= @publication.link %>
	</p>

	<br>
	<% if user_signed_in? && current_user.role == "admin"%>
	  <%= link_to 'Edit', edit_publication_path(@publication) %> | 
	<% end %>
	<%= link_to 'Back', publications_path %>

</div>
