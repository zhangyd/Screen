<%= form_for(@write) do |f| %>
  <% if @write.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@write.errors.count, "error") %> prohibited this write from being saved:</h2>

      <ul>
      <% @write.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.collection_select :person_id, @people, :id, :first_name, include_blank: true %>
  </div>
  
  <div class="field">
    <%= f.label :publication_id %><br>
    <%= f.collection_select :publication_id, @publications, :id, :title, include_blank: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>